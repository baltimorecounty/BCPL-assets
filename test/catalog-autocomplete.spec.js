/* eslint-disable no-undef */

describe('Catalog Autocomplete', () => {
	beforeEach((done) => {
		// loadFixtures('breadcrumbs.fixture.html');
		done();
	});

	it('should show the user a list of possible search results based on their input', () => {
		const actual = true;
		const expected = false;

		expect(actual).toEqual(expected);
	});
});
